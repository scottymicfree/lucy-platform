from __future__ import annotations

from backend.core.models import StructuredInput


def classify(structured: StructuredInput) -> StructuredInput:
    # Placeholder classifier hook.
    return structured
