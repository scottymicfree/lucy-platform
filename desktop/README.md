# Lucy Platform Desktop (Electron)

This folder contains the Electron desktop wrapper.

## Dev run (Windows)

From repo root:

```powershell
cd desktop
npm install
cd ..\frontend
npm install
cd ..
node desktop\dev.mjs
```

Backend must be runnable via `python backend/main.py`.
